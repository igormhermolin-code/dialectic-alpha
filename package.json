{
  "name": "dialectic-ai-debate-arena",
  "version": "0.1.0",
  "private": true,
  "description": "A dependency-free AI debate arena MVP with structured judging.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
