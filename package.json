{
  "name": "dialectic-ai-debate-arena",
  "version": "0.1.0",
  "private": true,
  "description": "An AI debate arena with structured judging and server-side persistence.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15"
  }
}
